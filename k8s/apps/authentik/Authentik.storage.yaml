apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
    name: authentik-bucket
    namespace: cc-office
spec:
    generateBucketName: authentik
    storageClassName: cc-s3-storage
    additionalConfig:
        maxSize: "20G"
